<link rel="stylesheet" href="/css/admin/statistical_chart.css">
<body>
    <div class="topnav">
        <a href="/"><img src="/public/logo.png" alt="Logo"></a>
        <a class="active" href="/Admin/Statistical">Thống kê</a>
        <a href="/Admin/Users">Tài khoản</a>
        <a href="/Admin/Novels">Tiểu thuyết</a>
        <a href="/Admin/Genres">Thể loại</a>
        <a href="/Admin/Transactions">Giao dịch</a>
        <a href="/Admin/Discussions">Bài thảo luận</a>
        <a href="/Admin/Reports">Báo cáo</a>
        <a class="active" href="/Admin/SubscriptionPlans">Gói đọc</a    >
            <div class="user-menu">
                <% if (typeof currentUser !== 'undefined' && currentUser) { %>
                    <% if (currentUser.role === 'admin') { %>
                        <a href="/Admin/Users">Quản lý</a>
                    <% } %>
                    <a href="/user/profile/<%= currentUser.id %>">Cá nhân</a>
                    <a href="/logout">Đăng xuất</a>
                <% } else { %>
                    <a href="/login">Đăng nhập</a>
                    <a href="/register">Đăng ký</a>
                <% } %>
            </div>
    </div>
    <div class="container mt-5">
        <h2 class="mb-4">📊 Thống kê thu nhập theo tháng</h2>
      
        <table class="table table-bordered table-hover">
          <thead class="thead-dark">
            <tr>
              <th>Tháng</th>
              <th>Tổng thu nhập (VNĐ)</th>
              <th>Số giao dịch</th>
            </tr>
          </thead>
          <tbody>
            <% if (monthlyIncome.length === 0) { %>
              <tr><td colspan="3">Không có dữ liệu giao dịch nào.</td></tr>
            <% } else { %>
              <% monthlyIncome.forEach(row => { %>
                <tr>
                  <td><%= row.Month %></td>
                  <td><%= row.TotalIncome.toLocaleString() %></td>
                  <td><%= row.TotalTransactions %></td>
                </tr>
              <% }) %>
            <% } %>
          </tbody>
        </table>
      </div>
</body>